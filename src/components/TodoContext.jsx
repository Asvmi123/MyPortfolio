import React from 'react'
import { ReactDOM } from 'react';


const Todo = () => {
  const [todo, setTodos] = useState([]);
  return (
    <TodoContext.Provider>
      {Children}
    </TodoContext.Provider>
  )
}

export default Todo;


